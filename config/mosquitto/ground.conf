# Ground-station broker.
#
# The far end of every boat's bridge. It originates no bridge of its own: the
# boats connect outward, because they are the side that loses the link.

listener 1883
allow_anonymous true

persistence true
persistence_location /var/lib/mosquitto/
autosave_interval 30
